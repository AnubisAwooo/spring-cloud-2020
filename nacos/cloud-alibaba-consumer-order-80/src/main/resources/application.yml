server:
  port: 80

spring:
  application:
    name: nacos-consumer-order
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
    loadbalancer:
      ribbon:
        enabled: false

service-url:
  nacos-user-service: 'http://nacos-provider-payment'
